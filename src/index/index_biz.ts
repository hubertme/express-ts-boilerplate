export default class IndexBiz {
    /**
     * Return the sum of two numbers (a and b)
     * @param a - First number
     * @param b - Second number
     */
    static sumTwoDigits(a: number, b: number) {
        return a + b;
    }
}
